# ══════════════════════════════════════════════════════════════════════════════
# Claude Code Mobile Controller - Environment Configuration
# ══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Generate AUTH_PASS_HASH with:
#   make hash-password PASS=yourpassword
# ══════════════════════════════════════════════════════════════════════════════

# ── Required ──────────────────────────────────────────────────────────────────

# Your domain pointing to this machine (must have DNS configured)
DOMAIN=claude.example.com

# Web UI login credentials
AUTH_USER=admin
AUTH_PASS=changeme
# Bcrypt hash of AUTH_PASS for Caddy (generate with: make hash-password PASS=changeme)
AUTH_PASS_HASH=

# Windows path to your code projects (will be mounted at /workspace in container)
WORKSPACE_PATH=C:/Users/me/projects

# ── Notifications ─────────────────────────────────────────────────────────────

# Pick something unique and random - this is your notification channel
# Anyone who knows this can send you notifications, so make it unguessable
# Example: claude-a7x9k2m4p1
NTFY_TOPIC=claude-CHANGEME

# Set to 'true' to use the self-hosted ntfy container instead of ntfy.sh
# When true, run with: docker compose --profile ntfy up -d
NTFY_SELF_HOSTED=false

# Optional: ntfy auth token for private topics
# Generate at https://ntfy.sh/account or on your self-hosted instance
NTFY_TOKEN=

# ── Claude Code ───────────────────────────────────────────────────────────────

# Your Anthropic API key (or leave blank to authenticate interactively)
ANTHROPIC_API_KEY=

# Path to your local Claude config directory (contains auth, settings, etc.)
# This gets mounted to /home/node_user/.claude in the container
CLAUDE_CONFIG_PATH=C:/Users/me/.claude

# Extra CLI flags to pass to Claude Code
# Example: --model claude-sonnet-4-20250514 --verbose
CLAUDE_OPTS=

# ── Tuning ────────────────────────────────────────────────────────────────────

# Minimum seconds between repeat notifications for the same prompt
DEBOUNCE_SECONDS=30

# ── Development ───────────────────────────────────────────────────────────────

# Environment mode: 'production' enables secure cookies (requires HTTPS)
# Leave unset or set to 'development' for local testing
NODE_ENV=production

# Set to 'true' to run in mock mode (simulates Claude Code output for testing)
MOCK_MODE=false
